By priority:
	Dev0 .odc

	semi-automatic generation of interface modules (by templates)

	Console:
		non-blocking (input)
		move to Cons?

	ConsCompiler:
		interface similar to DevCompiler

	handle SIGTERM in server version (when Kernel.Start used)

	CommTCP

	printing (to PostScript)

	GUI:
		...
